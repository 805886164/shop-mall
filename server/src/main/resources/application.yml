server.port: 9100
server.servlet.context-path: /api

BASE_LOCATION: D:\ideaNew2024\shop-mall
DB_NAME: shop

File:
  uploadPath: ${BASE_LOCATION}\server\upload

mybatis:
  configuration:
    map-underscore-to-camel-case: true


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shop-mall?useSSL=false&serverTimezone=UTC
    username: root
    password: taoqibao
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true